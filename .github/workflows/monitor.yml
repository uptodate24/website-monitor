name: Website Health Monitor
on:
  schedule:
    - cron: '*/5 * * * *'  # Setiap 5 menit
  workflow_dispatch:        # Bisa run manual

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
    - name: Check Mentari UNPAM
      run: |
        # Test dengan URL yang pasti DOWN dulu
        STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://invalid-website-test-12345.com")
        echo "Status Code: $STATUS_CODE"
        
        if [ "$STATUS_CODE" -ne 200 ]; then
          echo "🚨 Website DOWN - Sending Discord notification..."
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{\"content\":\"🔴 **TEST NOTIFICATION**\\nWebsite: https://invalid-website-test-12345.com\\nStatus: $STATUS_CODE\\nTime: $(date)\"}" \
            "${{ secrets.DISCORD_WEBHOOK }}"
          echo "✅ Notification sent to Discord"
        else
          echo "✅ Website UP - No notification sent"
        fi
    
    - name: Check Real Website (Backup)
      run: |
        STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://mentari.unpam.ac.id/")
        echo "Real Website Status Code: $STATCATCH_CODE"
        
        if [ "$STATUS_CODE" -ne 200 ]; then
          echo "🚨 Real Website DOWN - Sending Discord notification..."
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{\"content\":\"🔴 **MENTARI UNPAM DOWN**\\nURL: https://mentari.unpam.ac.id/\\nStatus: $STATUS_CODE\\nTime: $(date)\"}" \
            "${{ secrets.DISCORD_WEBHOOK }}"
        else
          echo "✅ Real Website UP"
        fi
