name: Mentari Monitor

on:
  schedule:
    - cron: '*/5 * * * *'   # tiap 5 menit
  workflow_dispatch:         # bisa run manual

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      # 🔹 1. Checkout repo dengan PAT
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      # 🔹 2. Cek status website
      - name: Check Mentari UNPAM
        id: check
        run: |
          # Baca status sebelumnya
          if [ -f "status.txt" ]; then
            PREV_STATUS=$(cat status.txt)
          else
            PREV_STATUS="UNKNOWN"
          fi
          
          echo "Previous Status: $PREV_STATUS"
          
          # Cek website
          START_TIME=$(date +%s)
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://mentari.unpam.ac.id/" --connect-timeout 15 --max-time 20 || echo "000")
          END_TIME=$(date +%s)
          RESPONSE_TIME=$((END_TIME - START_TIME))
          
          # Tentukan CURRENT_STATUS (harus DOWN/UP, bukan kode angka)
          if [ "$STATUS_CODE" = "200" ]; then
            CURRENT_STATUS="UP"
            echo "Website is UP - Status Code: $STATUS_CODE"
          else
            CURRENT_STATUS="DOWN"
            echo "Website is DOWN - Status Code: $STATUS_CODE"
          fi
          
          echo "STATUS_CODE=$STATUS_CODE" >> $GITHUB_ENV
          echo "CURRENT_STATUS=$CURRENT_STATUS" >> $GITHUB_ENV
          echo "PREV_STATUS=$PREV_STATUS" >> $GITHUB_ENV
          echo "RESPONSE_TIME=$RESPONSE_TIME" >> $GITHUB_ENV
          
          echo "📊 Status Comparison: Previous: $PREV_STATUS, Current: $CURRENT_STATUS, Code: $STATUS_CODE, Response Time: ${RESPONSE_TIME}s"

      # 🔹 3. Kirim notif UP jika berubah
      - name: Send Discord UP if changed
        if: env.CURRENT_STATUS == 'UP' && env.PREV_STATUS != 'UP'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          DISCORD_TIMESTAMP=$(date -u +%s)
          
          echo "🟢 Status berubah dari ${{ env.PREV_STATUS }} ke UP - Mengirim notif Discord..."
          
          curl -X POST -H "Content-Type: application/json" -d "{
            \"embeds\": [{
              \"title\": \"🟢 MENTARI UNPAM ONLINE\",
              \"description\": \"Website Universitas Pamulang kembali dapat diakses.\",
              \"color\": 3066993,
              \"thumbnail\": {\"url\": \"https://cdn-icons-png.flaticon.com/512/845/845646.png\"},
              \"fields\": [
                {\"name\": \"🔗 URL\", \"value\": \"[mentari.unpam.ac.id](https://mentari.unpam.ac.id)\", \"inline\": true},
                {\"name\": \"📊 Status Code\", \"value\": \"\`${{ env.STATUS_CODE }}\`\", \"inline\": true},
                {\"name\": \"⚡ Response Time\", \"value\": \"\`${{ env.RESPONSE_TIME }}s\`\", \"inline\": true},
                {\"name\": \"🔄 Status Sebelumnya\", \"value\": \"\`${{ env.PREV_STATUS }}\`\", \"inline\": true},
                {\"name\": \"🕐 Waktu\", \"value\": \"<t:${DISCORD_TIMESTAMP}:R>\", \"inline\": false}
              ],
              \"footer\": {\"text\": \"Mentari Monitor • GitHub Actions\", \"icon_url\": \"https://github.com/favicon.ico\"},
              \"timestamp\": \"${TIMESTAMP}\"
            }]
          }" "${{ secrets.DISCORD_WEBHOOK }}"

          echo "UP" > status.txt
          echo "✅ UP notification sent and status saved"

      # 🔹 4. Kirim notif DOWN jika berubah
      - name: Send Discord DOWN if changed
        if: env.CURRENT_STATUS == 'DOWN' && env.PREV_STATUS != 'DOWN'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          DISCORD_TIMESTAMP=$(date -u +%s)
          
          echo "🔴 Status berubah dari ${{ env.PREV_STATUS }} ke DOWN - Mengirim notif Discord..."
          
          case "${{ env.STATUS_CODE }}" in
            000) ERROR_MSG="Connection Failed / Timeout" ;;
            404) ERROR_MSG="Halaman Tidak Ditemukan" ;;
            500) ERROR_MSG="Server Error" ;;
            502) ERROR_MSG="Bad Gateway" ;;
            503) ERROR_MSG="Service Unavailable" ;;
            504) ERROR_MSG="Gateway Timeout" ;;
            *) ERROR_MSG="Koneksi Gagal / Error ${{ env.STATUS_CODE }}" ;;
          esac

          curl -X POST -H "Content-Type: application/json" -d "{
            \"embeds\": [{
              \"title\": \"🔴 MENTARI UNPAM OFFLINE\",
              \"description\": \"Website Universitas Pamulang tidak dapat diakses!\",
              \"color\": 15158332,
              \"thumbnail\": {\"url\": \"https://cdn-icons-png.flaticon.com/512/753/753345.png\"},
              \"fields\": [
                {\"name\": \"🔗 URL\", \"value\": \"[mentari.unpam.ac.id](https://mentari.unpam.ac.id)\", \"inline\": true},
                {\"name\": \"📊 Status Code\", \"value\": \"\`${{ env.STATUS_CODE }}\`\", \"inline\": true},
                {\"name\": \"❌ Error\", \"value\": \"\`${ERROR_MSG}\`\", \"inline\": true},
                {\"name\": \"⚡ Response Time\", \"value\": \"\`${{ env.RESPONSE_TIME }}s\`\", \"inline\": true},
                {\"name\": \"🔄 Status Sebelumnya\", \"value\": \"\`${{ env.PREV_STATUS }}\`\", \"inline\": true},
                {\"name\": \"🕐 Waktu\", \"value\": \"<t:${DISCORD_TIMESTAMP}:R>\", \"inline\": false}
              ],
              \"footer\": {\"text\": \"Mentari Monitor • GitHub Actions\", \"icon_url\": \"https://github.com/favicon.ico\"},
              \"timestamp\": \"${TIMESTAMP}\"
            }]
          }" "${{ secrets.DISCORD_WEBHOOK }}"

          echo "DOWN" > status.txt
          echo "🔴 DOWN notification sent and status saved"

      # 🔹 5. Status unchanged
      - name: Status unchanged
        if: env.CURRENT_STATUS == env.PREV_STATUS
        run: |
          echo "✅ Status unchanged: ${{ env.CURRENT_STATUS }} - No notification needed"
          echo "${{ env.CURRENT_STATUS }}" > status.txt

      # 🔹 6. Commit status changes dengan PAT
      - name: Commit status changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add status.txt
          git diff --staged --quiet || git commit -m "Update status: $(cat status.txt) [$(date +%Y-%m-%d\ %H:%M)]"
          git push
