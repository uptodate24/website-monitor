name: Mentari Monitor

on:
  schedule:
    - cron: '*/5 * * * *'  # tiap 5 menit
  workflow_dispatch:        # bisa run manual

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Restore previous status
      - name: Restore previous status
        uses: actions/cache@v3
        with:
          path: status.txt
          key: website-status

      # 2️⃣ Check Mentari UNPAM
      - name: Check Mentari UNPAM
        id: check
        run: |
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://mentari.unpam.ac.id/" --connect-timeout 10 --max-time 10)
          PREV_STATUS=$(cat status.txt 2>/dev/null || echo "UNKNOWN")
          echo "STATUS_CODE=$STATUS_CODE" >> $GITHUB_ENV
          echo "PREV_STATUS=$PREV_STATUS" >> $GITHUB_ENV
          echo "Status Code: $STATUS_CODE"
          echo "Previous Status: $PREV_STATUS"

      # 3️⃣ Send UP notification
      - name: Send Discord UP if changed
        if: env.STATUS_CODE == '200' && env.PREV_STATUS != 'UP'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          curl -X POST -H "Content-Type: application/json" -d "{\"embeds\":[{\"title\":\"🟢 Mentari UNPAM BACK UP\",\"description\":\"Website sudah kembali online.\",\"color\":3066993,\"fields\":[{\"name\":\"URL\",\"value\":\"https://mentari.unpam.ac.id/\",\"inline\":true},{\"name\":\"Status Code\",\"value\":\"$STATUS_CODE\",\"inline\":true},{\"name\":\"Time (UTC)\",\"value\":\"$TIMESTAMP\",\"inline\":false}]}]}" "${{ secrets.DISCORD_WEBHOOK }}"
          echo "UP" > status.txt

      # 4️⃣ Send DOWN notification
      - name: Send Discord DOWN if changed
        if: env.STATUS_CODE != '200' && env.PREV_STATUS != 'DOWN'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          curl -X POST -H "Content-Type: application/json" -d "{\"embeds\":[{\"title\":\"🔴 Mentari UNPAM DOWN\",\"description\":\"Website sedang tidak dapat diakses!\",\"color\":15158332,\"fields\":[{\"name\":\"URL\",\"value\":\"https://mentari.unpam.ac.id/\",\"inline\":true},{\"name\":\"Status Code\",\"value\":\"$STATUS_CODE\",\"inline\":true},{\"name\":\"Time (UTC)\",\"value\":\"$TIMESTAMP\",\"inline\":false}]}]}" "${{ secrets.DISCORD_WEBHOOK }}"
          echo "DOWN" > status.txt

      # 5️⃣ Status unchanged (opsional)
      - name: Status unchanged
        if: (env.STATUS_CODE == '200' && env.PREV_STATUS == 'UP') || (env.STATUS_CODE != '200' && env.PREV_STATUS == 'DOWN')
        run: echo "✅ Status unchanged - No notification needed"

      # 6️⃣ Save status to cache
      - name: Save status to cache
        uses: actions/cache@v3
        with:
          path: status.txt
          key: website-status
