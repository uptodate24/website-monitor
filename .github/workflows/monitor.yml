    - name: Check Mentari UNPAM
      run: |
        # Ganti sementara buat test
        STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://invalid-website-test-12345.com")
        echo "Status Code: $STATUS_CODE"
        
        if [ "$STATUS_CODE" -ne 200 ]; then
          echo "🚨 Sending Discord notification..."
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "{\"content\":\"🔴 **TEST NOTIFICATION**\\nWebsite: https://invalid-website-test-12345.com\\nStatus: $STATUS_CODE\\nTime: $(date)\"}" \
            ${{ secrets.DISCORD_WEBHOOK }}
        else
          echo "✅ Website UP"
        fi
